<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link href="/bitotsav/css/bootstrap.min.css" rel="stylesheet">
    <script src="/bitotsav/js/bootstrap.min.js"></script>
    
    
    <!--<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/jquery-ui.min.js"></script>-->
    
    
    
    
    <title>BIT Deets</title>

    <!-- Bootstrap -->
    
    <style type="text/css">
        body{
            background: url(bitotsav.jpg);
            background-repeat: no-repeat;
            background-size: cover;
        }
        .box{
            margin: 30px;
            padding: 20px;
            
            color: white;

        }
        .strong{
            margin: 30px;
            padding: 20px;
            
            color: white;

        }
        label{
            font-size: 20px;
            padding-right: 20px;

        }
        input{
            border: none;
            background-color: transparent;
            border-bottom: 1px solid gray;
            color: white;
            font-size: 20px;

        }
        .head{
            font-size: 50px;
            font-style: serif;
            padding-bottom: 20px;

        }
        .input-box{
            padding-bottom: 20px;
        }
        .login{
            padding-left: 250px;

        }
        .log-btn{
            border: 1px solid gray;
            border-radius: 5px;
            padding-right: 20px;
            padding-left: 20px;
        }
    </style>
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
  
      <div class="box">
    
          <div class="head">
              BITOTSAV'16 Core Member Login Page
          </div>
          
          <center>
      <div id="main-container" class="container">
      <div class="row">
        <% flash.each do |name, msg| -%>
        <%= content_tag :div, msg%>
        <% end %>
    </div>
    </div>
    </center>


          <%= form_tag(:action => 'attempt_login', :method => 'post') do %>
          <div class="input-box">
              <label>&nbsp;&nbsp;&nbsp;Team ID</label>
              <%= text_field_tag(:username, "") %>
          </div>

          <div class="input-box">
              <label>Passcode</label>
              <%= password_field_tag(:password, "") %>
          </div>
          <div class="input-box login">
              <%= submit_tag("Login", class: 'log-btn') %>
          </div>
          <% end %>
      </div>
  </body>
</html>