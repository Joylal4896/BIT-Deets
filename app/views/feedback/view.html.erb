<% for feed in @feedback %>
	<%= feed.name %>
	<%= feed.rollno %>
	<%= feed.email %>
	<%= feed.branch %>
	<%= feed.phoneno %>
	<%= feed.feedback %>
	<%= feed.putmore %>
	<%= feed.contactvia %>
	<br>
<% end %>
